<div class="container py-5">
  <%= link_to "← Home", root_path, class: "btn btn-outline-secondary mb-4" %>
  <h1 class="fw-bold mb-4">Choose a Topic</h1>
  <div class="row g-4">
    <% @topics.each do |topic| %>
      <div class="col-12 col-md-4">
        <div class="card h-100 shadow-sm border-0">
          <div class="card-body">
            <h5 class="card-title fw-bold"><%= topic.title %></h5>
            <p class="card-text text-muted"><%= topic.description %></p>
          </div>
          <div class="card-footer bg-white border-0">
            <%= link_to "Explore →", topic_path(topic), class: "btn btn-outline-primary w-100" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
